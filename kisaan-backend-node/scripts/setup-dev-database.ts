/**
 * DEPRECATED SCRIPT
 * Development database setup now uses:
 *   npm run schema:prepare   (structure)
 *   npm run db:bootstrap     (seed)
 * Optional full seed: npm run db:bootstrap:full
 */
console.warn('[DEPRECATED] setup-dev-database.ts replaced by schema:prepare + db-bootstrap flow.');
export {}; // noop