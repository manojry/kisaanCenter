/**
 * DEPRECATED SCRIPT
 * Unified seeding now handled by db-bootstrap.ts (--minimal-seed | --full-seed).
 * Replace usage with:
 *   npm run db:bootstrap        (minimal default)
 *   npm run db:bootstrap:full   (full seed)
 */
console.warn('[DEPRECATED] seed_all.js replaced by db-bootstrap.ts');
process.exit(0);
