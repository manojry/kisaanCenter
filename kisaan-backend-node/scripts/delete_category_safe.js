/**
 * DEPRECATED SCRIPT
 * Safe deletion / reassignment of categories is now handled via controlled domain
 * service operations with proper validation and audit trails.
 *
 * This stub remains only to prevent breaking any lingering npm/script references.
 * It performs no destructive action.
 */
console.warn('[DEPRECATED] delete_category_safe.js is removed. Use domain service (CategoryService) endpoints instead.');
process.exit(0);
