/**
 * DEPRECATED SCRIPT
 * Full arbitrary migration runner replaced by canonical schema:prepare flow.
 * Use: npm run schema:prepare (idempotent) or targeted migration scripts via run-migration.ts
 */
console.warn('[DEPRECATED] migrate_all.js replaced by schema:prepare & run-migration.ts');
process.exit(0);
