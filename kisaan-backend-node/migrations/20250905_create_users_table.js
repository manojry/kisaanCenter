 'use strict';

module.exports = {
	up: async (queryInterface, Sequelize) => {
		// This migration is intentionally left blank or deprecated.
		// You may safely remove this file if not needed.
		return Promise.resolve();
	},
	down: async (queryInterface, Sequelize) => {
		// This migration is intentionally left blank or deprecated.
		// You may safely remove this file if not needed.
		return Promise.resolve();
	}
};
