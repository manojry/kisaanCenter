# services/

This folder contains all business logic and service classes/functions for the Kisaan Backend Node project.

- Place reusable business logic here (e.g., userService, transactionService).
- Services should not handle HTTP requests directly.
