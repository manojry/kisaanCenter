// Removed: credits are now tracked as expenses with type 'advance'.
import { Router } from 'express';

const router = Router();

// All credit advance functionality has been moved to expenses module
// This router is kept for backward compatibility but contains no routes

export default router;
