* Always connect to real database that is running in aws. you can get the credentials from .env file in this folder
* Never generate and use dummy data in the python code. No Hardcoding of the data only use database to fetch and write the data
* Always refer to erd.md and models.py file and never assume anything
* Always keep erd, models, frontend, api's (backend) in sync. If you change something always make sure to that changes are reflected across all those areas appropriately.
* When deailing with owner type users keep these files Owner_features and Owner_enterprise_journey as base and do not divert from these requirements everytime.