The Python schema files that previously lived here (models.py, user_schemas.py, plan_schemas.py, subscription_schemas.py, shop_schemas.py) are deprecated.

Reason: Project standardized to a Node.js only stack. These Python files were legacy design artifacts and are now removed to prevent confusion.

If historical reference is needed, retrieve them from version control history.
